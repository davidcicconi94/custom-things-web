import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { ReactiveFormsModule } from '@angular/forms';

export const SHARED_STANDALONE_IMPORTS = [
    CommonModule,
    RouterModule,
    ReactiveFormsModule,
];
